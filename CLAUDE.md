# Puente para Claude Code

Leé `AGENTS.md` y usá la skill canónica en `.agents/skills/crm-multicanal/SKILL.md`.

Los comandos `/setup`, `/construi`, `/conecta`, `/integra`, `/deploy` y `/revisa`
delegan en esa misma fuente. Si hay una contradicción, manda `AGENTS.md`.
